from django.apps import AppConfig


class ExportSettingsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.export_settings"
    label = "export_settings"
    verbose_name = "Export Settings"
